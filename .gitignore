# deps / build
node_modules/
.next/
out/
dist/
*.tsbuildinfo
next-env.d.ts

# secrets
.env
.env.local
.env*.local

# database (runtime state — Git-as-DB는 PROGRESS.md/sentinel로, 바이너리 DB는 제외)
db.sqlite
db.sqlite-*

# per-run runtime artifacts (capture/generated) — 구조만 유지
runs/*/
!runs/.gitkeep

# clone-first 런타임 산출물 (루트 앵커 — app/captures 소스 라우트는 추적)
/captures/
/analysis/
/customizations/

# ralph loop local state
.claude/.ralph-loop.local.md
.harness/state/

# os / tooling
.DS_Store
playwright-report/
test-results/

# NOTE: screenshots/ 는 추적합니다 (화면 구현 피드백용 — doing.md)
